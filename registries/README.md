# Registries

Runtime registries are deployment-specific and private. This public reference intentionally omits live registry contents and machine identifiers. Implementations should create local registries from the schemas and assign their own node IDs, capabilities, and routing state.

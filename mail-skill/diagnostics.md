# Diagnostics

Check Keychain, DNS/TLS/IMAP authentication, mailbox selection, Message-ID, SMTP submission, IMAP delivery, attachment decode, idempotency, and callback queue separately. SMTP submission alone is not delivery proof.

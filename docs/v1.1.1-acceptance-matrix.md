# v1.1.1 Acceptance Matrix

| Gate | Requirement | Evidence | Result |
|---|---|---|---|
| A | manifest, UTF-8, bytes, SHA-256, markers | `artifact_handoff.py`, tests | PASS |
| A | manual fallback and provider abstraction | `ManualDownloadProvider` | PASS |
| A | large Markdown staging/consume/persistence | `test_large` | PASS |
| A | explicit recovery | `test_hash_mismatch` | PASS |
| B | declared mail capabilities | `mail-skill/SKILL.md` | NOT VERIFIED |
| B | real E2E and live receipts | authorized test mailbox required | BLOCKED |
| B | second employee clean install | machine unavailable | BLOCKED |

Conversation references are metadata only, never authoritative payloads.
# Gate C — Return-to-Origin reliability

Every dispatch persists `parent_gpt_conversation_id`, origin route, `task_id`,
generation, Codex thread/session, branch and timestamps. Successor bindings use
`inherit_task_id`; missing parents fail closed. Canonical envelopes carry
status, generation, body, thread, evidence, receipts, limitations, next action
and `callback_event_id`. Delivery is `PENDING → ACCEPTED → ACKED`; `OUTBOX`
holds unavailable routes. `ACCEPTED` records adapter acceptance only. The
current platform has `PLATFORM_BOUNDARY`: no supported adapter proves parent
GPT UI visibility, so Gate C cannot claim full end-to-end parent UI delivery.

Regression coverage: new thread and successor inheritance, route-unavailable
outbox recovery, callback idempotency and single ACK are in
`test_return_to_origin.py`.
## Gate C — Parent-GPT transport adapter (2026-09-03)

The release contract distinguishes `ACCEPTED`, `UI_VISIBLE`, and `GPT_ACKED`.
Local callback delivery, Office inbox, Codex deep-links, and new-task creation
cannot establish visibility in an existing parent GPT conversation.

`ParentGPTTransportAdapter` and injectable `ApplicationThreadMessageAdapter`
now enforce application-generated visibility evidence and an acknowledgement id.
Plain process success is rejected.

Current product finding: the host can send a message to an existing task through
the Codex application tool, but no supported local-process API was found that
invokes this capability and returns parent GPT UI visibility plus GPT ACK.
Gate C remains `PROTOCOL_PASS_PLATFORM_BOUNDARY`.
